#include <stdio.h>
#include <unistd.h>
#include <getopt.h>

char facetop[] = "  ____\n";
char face2nd[] = "/`    `\\\n";
char facemid[] = "| >  < |\n";
char face4th[] ="\\_.--._/\n";
char facelast[] ="_,)()(,_\n";

int main(int argc, char** argv)
{
    printf(facetop);
    printf(face2nd);
    printf(facemid);
    printf(face4th);
    printf(facelast);
    
    char testue[] = "uhoh"
    if (argv[0] != NULL)
        testue = argv[0];

    return 0;
}